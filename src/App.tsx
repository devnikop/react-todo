import React from "react";
import styled from "styled-components";

const App = () => {
  return (
    <StyledApp>
      <article></article>
    </StyledApp>
  );
};

const StyledApp = styled.header``;

export default App;
