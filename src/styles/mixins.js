const resetButton = () => `
  padding: 0;

  color: inherit;
  font: inherit;

  background-color: transparent;
  border: none;
  cursor: pointer;
`;

const resetList = () => `
  margin: 0;
  padding: 0;
  list-style: none;
`;

export { resetButton, resetList };
